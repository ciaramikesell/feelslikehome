export default function ComparePage() {
  return (
    <div style={{ fontSize: 13.5, color: 'var(--ink-soft)', padding: '30px 0', maxWidth: 480 }}>
      Compare mode — side-by-side homes with only the differences highlighted — is coming in a future round.
      For now, the Table view under <strong>Homes</strong> works well for eyeballing things side by side.
    </div>
  );
}
